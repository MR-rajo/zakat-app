<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="/css/output.css" rel="stylesheet" />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50 min-h-screen flex items-center justify-center">
    <div class="text-center">
      <div
        class="mx-auto h-24 w-24 bg-red-100 rounded-full flex items-center justify-center mb-6"
      >
        <i class="fas fa-exclamation-triangle text-red-600 text-3xl"></i>
      </div>

      <h1 class="text-6xl font-bold text-gray-900 mb-4">
        <%= error.status || 500 %>
      </h1>

      <h2 class="text-2xl font-semibold text-gray-700 mb-4">
        <%= title || 'Terjadi Kesalahan' %>
      </h2>

      <p class="text-gray-600 mb-8 max-w-md mx-auto">
        <% if (typeof message !== 'undefined' && message) { %> <%= message %> <%
        } else { %> Mohon maaf, terjadi kesalahan pada server. Silakan coba lagi
        nanti. <% } %>
      </p>

      <div class="space-x-4">
        <a href="/" class="btn-primary">
          <i class="fas fa-home mr-2"></i>Kembali ke Beranda
        </a>
        <button onclick="history.back()" class="btn-secondary">
          <i class="fas fa-arrow-left mr-2"></i>Kembali
        </button>
      </div>

      <% if (typeof process !== 'undefined' && process.env.NODE_ENV ===
      'development' && typeof error !== 'undefined' && error.stack) { %>
      <div class="mt-8 text-left max-w-4xl mx-auto">
        <details class="bg-red-50 border border-red-200 rounded-lg p-4">
          <summary class="cursor-pointer text-red-700 font-medium">
            Debug Information (Development Mode)
          </summary>
          <pre class="mt-4 text-xs text-red-600 overflow-auto">
<%= error.stack %></pre
          >
        </details>
      </div>
      <% } %>
    </div>
  </body>
</html>
